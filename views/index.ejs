<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="./stylesheets/main.min.css">
        <script src="./scripts/main.min.js" charset="utf-8"></script>
        <title>Giovanne Guerra</title>
    </head>
    <body>
        <nav class="navBar">
          <nav class="wrapper">
            <div class="logo"></div><!-- Logo -->
            <input type="checkbox" id="menu-toggle">
              <label for="menu-toggle" class="label-toggle"></label>
            </input>
            <ul>
              <a href="#"><li>Home</li></a>
              <a href="#"><li>About</li></a>
              <a href="#"><li>Skills</li></a>
              <a href="#"><li>Works</li></a>
              <a href="#"><li>Contact</li></a>
            </ul>
          </nav>
        </nav>
        <section class="hero section">
            <header class="container">
                <h1 class="hero__title"><%= __('Hello') %></h1>
                <p class="hero__text"><%= __('Language') %></p>
                <div class="block">
                    <% if(getLocale() !== 'en'){ %>
                        <a class="hero__item" href="/en"><%= __('English') %></a>
                    <% } %>
                </div>
                <div class="block">
                    <% if(getLocale() !== 'fr'){ %>
                        <a class="hero__item" href="/fr"><%= __('French') %></a>
                    <% } %>
                </div>
                <div class="block">
                    <% if(getLocale() !== 'pt-br'){ %>
                        <a class="hero__item" href="/pt-br"><%= __('Portuguese') %></a>
                    <% } %>
                </div>
                <div class="block">
                    <form class="" action="/" method="post">
                        <input type="text" name="form[name]" placeholder="name">
                        <input type="text" name="form[surname]" placeholder="surname">
                        <input type="text" name="form[age]" placeholder="age">
                        <input type="submit" name="submit" value="enviar">
                    </form>

                </div>
            </header>
        </section>
    </body>
</html>
